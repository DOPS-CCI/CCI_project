<Window x:Class="ConvertEEG2FM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" FontSize="14" FontWeight="Bold">
    <Grid>
        <StackPanel Orientation="Vertical">
        <ScrollViewer Height="250" Margin="8">
                <ListBox Name="GVSelection" SelectionMode="Multiple"></ListBox>
            </ScrollViewer>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Height" Value="32"/>
                    <Setter Property="Width" Value="80"/>
                    <Setter Property="Margin" Value="8"/>
                </Style>
            </StackPanel.Resources>
            <Button Click="OpenFile_Click" >Open file</Button>
            <Button Click="Convert_Click">Convert</Button>
            <Button Click="Quit_Click">Quit</Button>
        </StackPanel>
        </StackPanel>
    </Grid>
</Window>
