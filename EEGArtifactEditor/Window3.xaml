<Window x:Class="EEGArtifactEditor.Window3"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        ResizeMode="CanResizeWithGrip" Width="500" Height="Auto" Closing="Window_Closing" WindowStartupLocation="CenterScreen" Topmost="True"
        WindowStyle="ToolWindow" Title="Replace or create?">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap" FontSize="18">You have modified an existing dataset which has previously had artifacts removed. Do you want to replace the existing files
            or create a new file? This affects only the HDR or EVT files in the original dataset.</TextBlock>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="Height" Value="28"/>
                    <Setter Property="Width" Value="100"/>
                    <Setter Property="Margin" Value="8"/>
                </Style>

            </StackPanel.Resources>
            <Button Name="Replace" Click="Replace_Click" IsDefault="True">Replace</Button>
            <Button Click="Replace_Click">Create new</Button>
        </StackPanel>
    </Grid>
</Window>
